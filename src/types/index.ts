/**
 * Central type definitions export
 * Import types from this file for cleaner imports: import { Paper } from '@/types'
 */

export type { Paper, Publication, PaperWithAbstract } from './paper';

// MUI theme extensions are declared globally via module augmentation
// No need to export them - they're automatically available
